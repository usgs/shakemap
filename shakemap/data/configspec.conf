[gmpe_modules]
    __many__ = string_list(min=2, max=2)

[ipe_modules]
    __many__ = string_list(min=2, max=2)

[gmice_modules]
    __many__ = string_list(min=2, max=2)

# Cross correlation modules
[ccf_modules]
    __many__ = string_list(min=2, max=2)

[gmpe_sets]
    [[__many__]]
        gmpes = string_list(min=1)
        weights = float_list(min=1)
        weights_large_dist = float_list(min=0)
        dist_cutoff = float(min=0)
        site_gmpes = string_list(min=0)
        weights_site_gmpes = float_list(min=0)

[grind]
    # List of intensity measure types
    # Supported values are "PGA", "PGV", "Sa(0.3)", "Sa(1.0)", "Sa(3.0)"
    imt_list = string_list(min=1)

    gmice = string(max=100)
    gmpe = string(max=100)
    ipe = string(max=100)
    ccf = string(max=100)

    [[bias]]
        max_range = float(min=0)
        max_mag = float(min=0, max=10)
        max_delta_sigma = float(min=0)

    # Outlier flagging
    [[outlier]]
        max_deviation = float(min=0)
        max_mag = float(min=0, max=10)

    source_network = string(min=1)

    component = string(min=1)

    xres = string(min=1)
    yres = string(min=1)
# End [grind]

