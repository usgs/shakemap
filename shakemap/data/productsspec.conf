[products]
  [[contour]]
    filter_size = integer()
  [[mapping]]
    operator = string()
    max_workers = integer(min=0, default=8)
    [[[layers]]]
      coasts = file_type()
      oceans = file_type()
      lowres_oceans = file_type()
      lakes = file_type()
      countries = file_type()
      states = file_type()
      topography = file_type(default='')
      cities = file_type()
    
